// SPDX-License-Identifier: AGPL-3.0-or-later
import { createExtension } from '../createExtension';

export const metaLabelsExtension = createExtension('meta_labels', {
  displayName: 'Labels',
  description: 'Tag and label management for entities',
});
